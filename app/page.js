export default function Home() {
  return (
      <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
            <h1>Lokia Sales Bot</h1>
                  <p>Telegram bot is running. Webhook endpoint: <code>/api/webhook</code></p>
                        <p>Send endpoint: <code>/api/send</code></p>
                            </main>
                              );
                              }
